import React from "react";

function Buttons(){
    return(
        <div>
        <button>Back</button>
        <button type="submit">Save and Continue</button>
        </div>
    )
}

export default Buttons
